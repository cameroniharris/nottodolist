
<div class="booyah-box col-xs-10 col-xs-offset-1">
  <h1><%= @list.name %></h1>
    <div class="pull-left">
        <%= link_to 'Edit', edit_list_path(@list), :class => 'btn btn-primary' %>
        <%= link_to 'Destroy', list_path(@list), :method => :delete, :data => {:confirm => 'Are you sure you want to delete this?'},:class => 'btn btn-danger' %>
     </div>
</div>